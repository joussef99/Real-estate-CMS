// Shared by Project and Resale admin/public forms so the same fixed set of
// finishing states is offered everywhere (no free-text "Other" — this is a
// closed list unlike compound/unit-type dropdowns).
export const FINISHING_STATUS_OPTIONS = ['Fully Finished', 'Core & Shell', 'Half Finished'];
