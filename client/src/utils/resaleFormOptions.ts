// Individual resale units (unlike project-wide bedroom ranges) commonly
// include studios, so this is a superset of the BEDROOM_OPTIONS used in the
// Projects/Resale search filters — kept separate to avoid changing that
// already-shipped, verified filter behavior.
export const RESALE_BEDS_OPTIONS = ['Studio', '1', '2', '3', '4', '5+'];
